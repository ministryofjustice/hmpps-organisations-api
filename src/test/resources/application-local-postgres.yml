spring:
  datasource:
    url: 'jdbc:postgresql://localhost:${POSTGRES_TEST_DB_PORT:-5432}/organisations-db'
    username: organisations
    password: organisations
  flyway:
    locations: classpath:/migrations/common,classpath:/migrations/test
    placeholders:
      dpr_user: dpr_user
      dpr_password: dpr_password