server:
  shutdown: immediate

spring:
  jpa:
    show-sql: true

  datasource:
    url: 'jdbc:postgresql://localhost:5432/organisations-db'
    username: organisations
    password: organisations

  flyway:
    locations: classpath:/migrations/common,classpath:/migrations/test

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

api:
  base:
    url:
      hmpps-auth: "http://localhost:8090/auth"
